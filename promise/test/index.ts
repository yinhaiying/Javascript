import * as chai from "chai";
const assert = chai.assert;
import * as mocha from "mocha"

describe('Chai的使用', () => {
    it('可以测试相等',() => {
        assert(1===1);
    })
    it('是一个类',() => {
      
    });
    it("拥有对象方法then",() => {
      
    })
});
